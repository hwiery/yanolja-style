# 야놀자 스타일 프로젝트 전체 체크리스트

## 1. 프로젝트 설정  
- [x] Next.js 14 프로젝트 초기화  
- [x] TypeScript 및 `tsconfig.json` 설정  
- [x] TailwindCSS 및 PostCSS 설정  
- [x] `.env.local`에 환경변수 등록  
  - `DATABASE_URL` (Railway PostgreSQL)  
  - `UNSPLASH_ACCESS_KEY` 또는 기타 CDN 키  

## 2. 프론트엔드 구조 및 최적화  
### 2.1 컴포넌트 구조  
- [x] `components/` 폴더로 컴포넌트 이동 및 구조화  
- [x] `WLight.tsx` 분리 및 재사용 가능한 UI 컴포넌트(`Button`, `Card`, `TabButton`, `ImageSlider`) 추출  
- [x] 레이아웃 컴포넌트 최적화 (AppLayout, MainLayout 등)  

### 2.2 스타일링 & 반응형  
- [x] 외부 이미지 → 로컬 또는 Next.js `Image` 컴포넌트로 전환  (Category, Nav, Card 컴포넌트 적용 완료)  
- [x] 모바일·태블릿·데스크탑 반응형 레이아웃 검증 및 보완  
- [x] TailwindCSS 테마(색상, 폰트 등) 커스터마이징  

### 2.3 접근성 & 퍼포먼스  
- [x] 모든 이미지 `alt` 속성 추가  
- [x] 시맨틱 HTML 요소 및 ARIA 속성 적용  
- [x] 불필요한 리렌더링 방지 (`React.memo`, `useMemo`, `useCallback`)  
- [x] 코드 스플리팅 및 지연 로딩(dynamic import)  

## 3. 백엔드 연동 준비  
### 3.1 인프라  
- [x] Railway에 프로젝트 생성 및 PostgreSQL 데이터베이스 추가  
- [x] 로컬 `.env.local`에 `DATABASE_URL` 설정  

### 3.2 데이터베이스 설계  
- [x] `accommodations` 테이블 스키마 정의  
  - `id` (UUID, PK)  
  - `name`, `location`, `description`, `price`, `image_url`  
  - `created_at`, `updated_at`  
- [x] 추가 테이블 설계 (categories, amenities, reviews 등)  

### 3.3 테스트 데이터 시드  
- [x] 더미 숙소 10건 CSV 작성  
  - 컬럼: `name`, `location`, `description`, `price`, `image_url` 또는 `image_file`  
- [x] 이미지 파일 준비 (로컬 업로드 / Unsplash URL / Vercel Asset@edge)  
- [x] 시드 스크립트 작성 및 실행 (`npm run seed`)  

## 4. 백엔드 API 구현  
- [x] Next.js API Routes 또는 Express.js 설정  
- [x] Prisma 또는 TypeORM으로 DB 커넥션 모듈 작성  
- [x] GET `/api/accommodations` (목록) 엔드포인트 개발  
- [x] GET `/api/accommodations/:id` (상세) 엔드포인트 개발  
- [x] 입력 검증·에러 처리 (OWASP 가이드 준수)  

## 5. 프론트엔드 데이터 연동  
- [x] API 호출 헬퍼 함수 작성 (`fetchAccommodations`, `fetchAccommodationById`)  
- [x] 섹션1: 대표 숙소 그리드 (4개 항목) 컴포넌트 구현  
- [x] 섹션2: 추천 숙소 슬라이더 또는 카드 리스트 구현  
- [x] 로딩·에러 UI(스피너, 토스트) 구현  
- [x] TypeScript 인터페이스 정의  

## 6. 기능 및 상호작용 구현  
- [x] 카테고리 필터링 UI 및 기능 구현  
- [x] 검색 기능 구현 (입력, 디바운스, API 연동)  
- [x] 정렬(가격, 평점 등) 기능 구현  
- [x] 예약 버튼 클릭 시 상세 페이지/모달 전환  

## 7. 테스트 및 품질 보증  
- [x] 단위 테스트 설정 및 작성 (Jest, React Testing Library)  
- [x] E2E 테스트 시나리오 작성 및 실행 (Cypress)  
- [x] 로컬 및 Railway 프로덕션 환경에서 API 정상 동작 검증  
- [x] 보안 검토 (CORS, 입력 검증, HTTP 헤더 등)  

## 8. 배포 및 운영  
- [ ] Railway에 백엔드 배포  
- [ ] Vercel에 프론트엔드 배포  
- [ ] 환경변수 재확인 및 설정  
- [x] 로그·오류 모니터링 설정 (Sentry 등)  

## 9. 문서화 및 향후 과제  
- [x] README 업데이트 (프로젝트 개요, 설치, 시드, 배포 가이드)  
- [x] 운영 워크플로우 문서 작성 (CSV → 시드 → 배포)  
- [ ] 어드민 페이지/CRUD 기능 확장 계획 수립   